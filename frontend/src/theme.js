import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#296c00",
    },
    secondary: {
      main: "#93fb5f",
    },
    blue: {
      main: "#00296C",
    },
    error: {
      main: "#6C0029",
    },
  },
});

export default theme;
